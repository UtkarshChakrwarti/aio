apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - mysql-app.yaml
  - airflow-app.yaml
  - monitoring-app.yaml

labels:
  - pairs:
      app: gitops-poc
    includeSelectors: false
