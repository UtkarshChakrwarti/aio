apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: airflow-app
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io/cleanup
spec:
  project: default
  source:
    repoURL: https://github.com/UtkarshChakrwarti/aio.git
    path: k8s/airflow/overlays/dev
    targetRevision: main
  destination:
    server: https://kubernetes.default.svc
    # No namespace — resources span airflow-core and airflow-user
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
